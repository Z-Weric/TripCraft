# TripCraft Design System — Postcard

## 1. 视觉主题与情绪

旅途中寄回的一张明信片。暖米色纸质底，衬线标题，赤陶橙重音色。旅行的温度感来自纸质感而非屏幕装饰。信息以编辑式排版排列，留白像信纸的呼吸。

**记忆点**：纸的温度——用户打开页面就像拆开一封旅行来信。

## 2. 色彩角色与对比规则

| 角色 | 色值 | 用途 |
|---|---|---|
| 纸底 | `#FBF7F0` | 页面主背景 |
| 卡片底 | `#FFFFFF` | 搜索栏、图表区域 |
| 主文 | `#2C2418` | 正文、标题 |
| 次文 | `#6B5B4A` | 备注、辅助说明 |
| 弱文 | `#8B7B6A` | 时间、标签文字 |
| 重音 | `#C9622A` | 按钮、激活态、价格、时间线节点 |
| 重音浅 | `#E8DCC8` | 分隔线、边框、未激活标签 |
| 分类色-自然 | `#5B8C5A` | 自然风光标签 |
| 分类色-美食 | `#C9622A` | 美食标签（与重音同色） |
| 分类色-历史 | `#8B6F4E` | 历史文化标签 |
| 分类色-购物 | `#B8860B` | 购物标签 |
| 成功 | `#5B8C5A` | 预算内、验证通过 |
| 警告 | `#C9622A` | 超支警告、验证失败 |

**对比规则**：主文 `#2C2418` on `#FBF7F0` = 11.8:1（AAA）。次文 `#6B5B4A` on `#FBF7F0` = 6.4:1（AA+）。弱文 `#8B7B6A` on `#FBF7F0` = 3.9:1，仅用于 >14px 非关键信息。

## 3. 字体角色与尺度

| 角色 | 字体 | 字号 | 字重 | 行高 |
|---|---|---|---|---|
| 品牌名 | Georgia, "Songti SC", serif | 22px | 700 | 1.2 |
| 日标题 | Georgia, "Songti SC", serif | 28px | 700 | 1.2 |
| 景点名 | Georgia, "Songti SC", serif | 18px | 600 | 1.3 |
| 章节标题 | 系统无衬线 | 15px | 600 | 1.4 |
| 正文 | 系统无衬线 | 14px | 400 | 1.6 |
| 备注 | 系统无衬线 italic | 13px | 400 | 1.6 |
| 标签 | 系统无衬线 | 13px | 400 | 1.4 |
| 时间/数字 | "SF Mono", "Roboto Mono", monospace | 12-14px | 400-600 | 1.4 |

**系统无衬线栈**：`-apple-system, BlinkMacSystemFont, "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", "Noto Sans SC", sans-serif`

**中文标题字重**：只用 600-700，不写 `bold`（避免伪合成发虚）。

## 4. 间距、网格与构图

- **基准单位**：4px。间距序列：4, 8, 12, 16, 20, 24, 32, 40, 48, 64。
- **内容最大宽度**：800px（行程区），640px（搜索区）。
- **页面内边距**：桌面 32px，移动 16px。
- **网格**：单列为主，行程时间轴左侧 24px 缩进给时间线轨道。
- **不对称**：搜索栏居中但行程区左对齐，章节标题左对齐不居中——编辑式排版。

## 5. 组件表面与状态

| 组件 | 表面 | 边框 | 圆角 | 阴影 |
|---|---|---|---|---|
| 搜索栏 | `#FFFFFF` | 2px solid `#C9622A` | 2px | 无 |
| 输入框 | transparent | 无（容器统一） | 无 | 无 |
| 偏好标签-默认 | transparent | 1px solid `#D4C4A8` | 20px | 无 |
| 偏好标签-激活 | `#C9622A` | 1px solid `#C9622A` | 20px | 无 |
| 主按钮 | `#C9622A` | 无 | 2px | 无 |
| 时间线节点 | `#FBF7F0` | 2px solid `#C9622A` | 50% | 无 |
| 时间线轨道 | — | 2px solid `#E8DCC8` | 无 | 无 |
| 日分隔线 | — | 2px solid `#2C2418` | 无 | 无 |

**交互状态**：
- 按钮 hover：背景 `#B55220`（赤陶暗一档）
- 按钮 active：背景 `#A04818`
- 按钮 focus-visible：`outline: 2px solid #C9622A; outline-offset: 2px`
- 标签 hover：边框色变为 `#C9622A`
- 输入 focus：底边框变 `#C9622A`（搜索栏内部）

## 6. 深度、边框与图像

- **无阴影策略**：明信片风格依赖纸质质感而非深度。唯一例外是页面级容器的极轻投影 `0 2px 12px rgba(44,36,24,0.06)`。
- **边框**：使用实线或虚线，颜色取 `#E8DCC8`（浅纸边）或 `#2C2418`（强分隔）。
- **图像**：地图区域用 Leaflet 默认浅色底，不需额外处理。如未来有景点照片，使用直角无圆角，像贴在明信片上的照片。

## 7. Do / Don't

**Do**：
- 用衬线字体做标题和景点名，传递编辑式旅行气质
- 用等宽字体做时间和数字，保证对齐和可读
- 保持大量纸质留白
- 用赤陶橙做唯一重音色，控制使用面积

**Don't**：
- 不用圆角超过 4px 的容器（标签的 20px 胶囊除外）
- 不用阴影制造深度感
- 不用渐变
- 不用 emoji 做界面图标
- 不用纯黑文字（`#2C2418` 是最深色，不用 `#000`）
- 不用紫蓝科技色调

## 8. 响应式行为

- **桌面 ≥768px**：行程区 800px 居中，搜索区 640px 居中，地图与花费图表上下排列。
- **移动 <768px**：全宽单列，内边距 16px，偏好标签换行，地图高度 240px，花费图表全宽。

## 9. 动效哲学

动效服务状态切换，不做装饰。

- **生成加载**：按钮文字变为"生成中..."，按钮不可重复点击。搜索区下方显示赤陶橙线性进度条。
- **行程展开**：每日行程初始即展开，不做折叠动画。如行程过长，Day 2+ 可折叠但展开是瞬时无动画。
- **地图交互**：点击时间轴景点 → 地图对应标记高亮（flyTo + 弹窗）。这是有意义的状态联动动效。
- **图表渲染**：ECharts 饼图入场动画 600ms，之后无循环动画。
- **reduced-motion**：所有过渡降为 `0.01ms`，地图 flyTo 降为 jumpTo。